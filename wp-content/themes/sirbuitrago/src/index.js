// import NAME from './react/NAME';

// const ELEMENT = document.querySelector(ELEMENT);

// if(ELEMENT) {
//     const links = ELEMENT ? ELEMENT.dataset.links : [];
//     ReactDOM.render( <NAME />, ELEMENT );
// }